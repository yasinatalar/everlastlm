node_modules/
dist/
build/
.next/
out/
coverage/
*.tsbuildinfo

.env
.env.local
.env.*.local
# Anything derived from an env file — .env.local.bak, .env.production.backup,
# .env.save — is just as sensitive as the original and matches none of the
# patterns above.
.env*.bak
.env*.backup
.env*.save
.env*.orig
!.env.example

.DS_Store
*.log
npm-debug.log*
pnpm-debug.log*

supabase/.branches/
supabase/.temp/
.vercel
.turbo
